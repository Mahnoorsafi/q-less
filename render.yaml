services:
  - type: web
    name: qless-ai
    env: python
    buildCommand: pip install -r requirements.txt && python generate_seed_data.py
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
